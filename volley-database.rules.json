{
  "rules": {
    "volley": {
      ".read": "auth != null",

      "users": {
        "$key": {
          ".write": "auth != null && newData.exists()",
          "name": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 30"
          },
          "ph": {
            ".validate": "!data.exists() || newData.val() === data.val()"
          }
        }
      },

      "players": {
        "$pid": {
          ".write": "auth != null",
          "name": {
            ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 40"
          }
        }
      },

      "sessions": {
        "$sid": {
          ".write": "auth != null",
          "title":  { ".validate": "newData.isString() && newData.val().length <= 80" },
          "status": { ".validate": "newData.isString() && (newData.val() === 'upcoming' || newData.val() === 'done')" },
          "mvp": {
            "open": { ".validate": "newData.isBoolean()" },
            "votes": {
              "$key": { ".validate": "newData.isString() && newData.val().length <= 40" }
            }
          }
        }
      },

      "videos": {
        "$vid": {
          ".write": "auth != null",
          "title": { ".validate": "newData.isString() && newData.val().length <= 80" },
          "type":  { ".validate": "newData.isString() && (newData.val() === 'link' || newData.val() === 'file')" }
        }
      },

      "saved": {
        "$key": {
          ".write": "auth != null",
          "$vid": { ".validate": "newData.isBoolean()" }
        }
      }
    }
  }
}
